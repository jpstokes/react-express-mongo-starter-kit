# react-express-mongo-starter-kit

This kit gets your started with a simple react, express, mongo setup.

Right now, you'll need to start the client:

    cd client && (yarn || react) start

Start server with:

    (yarn || npm) start
